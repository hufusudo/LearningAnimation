# 演示项目 Agent 开发规范 (agent.md)

> 本目录用于存放高等数学、概率论与数理统计、计算机专业基础（408）等学科的交互式可视化教学与几何直观系统。
> 所有在此目录下创建或维护演示项目的 Agent，必须遵循以下核心规范：

---

## §1 学科层级与门厅导航规范 (核心新增)

### 1.1 学科分类矩阵 (Discipline Taxonomy)
所有演示项目必须具有明确的学科归属，严禁无序平铺或脱离学科分类。当前基石学科划分为：
1. **高等数学 (`math`)**：
   - 板块名称严格定为**“高等数学”**（严禁附加“与空间解析几何”等长后缀）；
   - 涵盖微积分中值定理、空间异面直线距离与投影、旋转曲面与二次圆锥面消参推导等。
2. **概率论与数理统计 (`prob`)**：
   - 涵盖多维随机变量联合分布曲面、切片截面面积求边缘密度（祖暅原理）、条件分布归一化等。
3. **数据结构 (`ds`)**：
   - 计算机考研 408 核心子学科之一；
   - 涵盖外部排序（置换-选择排序与最佳归并树）、败者树、哈夫曼树等经典算法与数据结构。
4. **计算机组成原理 (`co`)**：
   - 计算机考研 408 核心子学科之一；
   - 涵盖单总线 CPU 数据通路控制信号与微操作时钟演进、顺序乘法移位器与 [ACC, MQ] 联合移位机制等硬件微架构。
5. **计算机操作系统 (`os`)**：
   - 计算机考研 408 核心子学科之一；
   - 涵盖系统引导流程（BIOS → MBR → DBR → Kernel 接力链路）、内存动态分区管理（伙伴系统 Buddy System 二叉折半切分与链表动态迁移）等核心机制。
6. **计算机网络 (`net`)**：
   - 计算机考研 408 核心子学科之一；
   - 涵盖滑动窗口可靠传输（GBN / SR 协议与时空交互梯形图）、数据链路与交换技术（以太网 VLAN 划分与 802.1Q 打标中继、以太网交换机自学习与帧转发表）等。
7. **计算机专业基础综合 - 408 全科 (`cs`)**：
   - 作为聚合视图，包含上述 `ds`、`co`、`os`、`net` 的全部 9 个核心模块。
8. **未来扩展学科**（如线性代数 `linalg`、离散数学 `discrete` 等）：新增学科时需同步扩充学科元数据。

### 1.2 两级门厅导航流程 (Two-Tier Entrance Navigation)
展示门厅 (`show/index.html`) 必须维持**“先选学科，再看演示”**的清晰层级体验，并贯彻**极简克制美学**：
- **门厅顶栏规范 (Ultra-Minimalist Header)**：
  - 站点品牌名称统一严格命名为**“可视化实验室”**；
  - 严禁在标题周围附带英文子标题、冗余修饰语或额外标签（如“按学科浏览”、“Interactive Visualizer Lab”等）；
  - 右侧仅保留纯粹的功能切换按键（“学科大厅”、“全部模块”），严禁放置外部风格标签（如“Taste-Skill Editorial”等）。
- **一级入口（学科大厅 Discipline Hub - `#hub`）**：
  - 用户进入门厅时，默认展示**6大学科大类导航卡片**（高等数学、概率统计、数据结构、计组原理、操作系统、计算机网络）；
  - **Banner 区域极简化**：仅保留“选择学科进入专题演示”主标题与轻量模块计数（“6 大学科门类 · 14 个独立系统”），严禁添加冗长导言段落或多余的大写英文分类标签；
  - **学科卡片极简与清单化规范（核心设计要求）**：
    - **板块名称规范**：高等数学、概率论与数理统计、数据结构、计算机组成原理、计算机操作系统、计算机网络；
    - **严禁冗余段落描述**：学科大类卡片内**不需要任何文字性描述**，严禁堆砌长篇空泛的简介段落，杜绝视觉干扰与信息冗余；
    - **直观列出收录模块名称**：卡片内部主体必须**直接清晰列出该学科内所收录的所有演示模块的具体名称**（采用精致小胶囊条目呈现，带圆点指示与截断省略），使用户无需进入二级页面即可一眼扫视该学科包含的完整内容列表；
    - 卡片右上方收录数量徽章必须设置 `whitespace-nowrap flex-shrink-0`，杜绝在窄屏或长标题下产生非预期的文字折行；
    - 卡片配备清晰的学科英文角标及“进入学科展厅 &rarr;”交互指引；
  - 底部保留轻量“展开全部 14 个独立演示卡片”与“计算机408统考全科综合展厅 (9 个模块)”快速通道。
- **二级展厅（学科专属展厅 Discipline Showcase - `#<discipline-id>`）**：
  - 用户点击学科卡片进入该学科，展示**面包屑导航**（`← 返回学科大厅 / 当前学科`）；
  - 顶部配备**学科快速切换胶囊栏**（全部 / 高等数学 / 概率统计 / 数据结构 / 计组原理 / 操作系统 / 计算机网络 / 408全科）；
  - 展示该学科专属 Header（包含学科名称与当前模块数量统计，保持极简，不展示空泛描述）；
  - 仅过滤展示该学科下的详细演示卡片列表（含公式、微操作/动画特性与“进入演示”链接），并使用 GSAP 进行平滑交错入场动效；
  - 页面必须支持 URL Hash 路由（`#hub`, `#math`, `#prob`, `#ds`, `#co`, `#os`, `#net`, `#cs`, `#all`），确保刷新与外链可精准直达特定学科。

### 1.3 展厅集成自动化标准 (Portal Integration Specification)
新建或维护演示项目时，必须在 `show/index.html` 中同步完成三处挂载：
1. **二级网格挂载**：在 `#projects-grid` 中添加符合规范的卡片，并标记具体学科标识 `data-discipline="<学科标识>"`（如 `math`, `prob`, `ds`, `co`, `os`, `net`）；
2. **学科元数据维护**：在页面内 `disciplineMeta` 对象中，递增对应学科的模块计数（`count`），保持 `desc` 为空；
3. **一级卡片清单同步**：在一级学科大厅对应的学科大类卡片上，更新模块数量徽章，并在模块名称列表中追加新增模块的名称条目（保持纯模块名呈现，禁止插入段落描述）。

---

## §2 核心交互与动画规范

- **可互动且丝滑连续（拒绝离散突变）**：
  - 画布中的几何图元、切片平面、参数滑块、空间动点等，必须支持 60fps 连续跟手交互（原生 Pointer Events）；
  - 拖拽与滑动过程中，截交线、截面多边形面积、投影指示线、辅助线与动态公式数值必须以 60fps 实时重算渲染，严禁离散帧卡顿跳转。
- **GSAP 缓动驱动**：
  - 预设视角重置、一键捕捉特征点、自动扫描切片/巡航演示等动效，统一使用 GSAP（如 `power2.out` / `power3.out`）进行平滑缓动补间，严禁突兀生硬的变化。

---

## §3 界面与视觉设计规范 (Taste-Skill Light Editorial)

- **严格浅色纸质美学**：
  - 全站严格采用浅色主题（温暖纸白 `#FAF9F5` 底色，纯白 `#FFFFFF` 卡片，精致 1px 细线边框 `#E5E4DC`）；
  - 严禁深色暗黑模式、泛滥的 generic AI 紫色渐变或厚重阴影。
- **排版与字体层级**：
  - 界面 UI：现代无衬线体（`-apple-system`, `SF Pro Display`, `Segoe UI`, `Roboto`）；
  - 数学符号与定理：经典衬线体（`Newsreader`, `Georgia`）；
  - 坐标与数值：等宽字体（`JetBrains Mono`, `monospace`）；
  - 数学公式：统一采用 KaTeX CDN 动态渲染。
- **笔记式引用排版 (Obsidian-Style Callouts)**：
  - `[!tip]` 翡翠绿 / `[!note]` 湖水蓝 / `[!important]` 熏衣紫 / `[!warning]` 琥珀黄。
- **交互微组件与微动效规范 (Taste-Skill UI & Micro-Interactions)**：
  - **色彩脱敏与纸质收敛**：借鉴外部开源组件（如 Galaxy / Uiverse）时，严禁使用深色暗黑背景、霓虹光晕与高饱和度流光渐变。必须脱敏清洗为暖白纸面（`#FAF9F5`）、纯白卡片（`#FFFFFF`）与 1px 细线边框（`#E5E4DC`）；功能色严格限定在琥珀金（`#D97706`）、翡翠绿（`#059669`）、钴蓝（`#2563EB`）与石板灰（`#57534E`）。
  - **物理触感微按键 (Tactile Micro-Buttons)**：播放、单步、复位等核心操作键必须具备机械物理键程感（`border border-[#E5E4DC] bg-white shadow-[0_1.5px_0_#D6D3CD] active:shadow-none active:translate-y-[1.5px] transition-all duration-150`），杜绝粗糙扁平或生硬突变。
  - **状态呼吸脉冲指示灯 (Pulse Indicators)**：硬件总线占用、内存读写、网络碰撞或 TLB 命中等动态状态，统一采用纯 CSS 细微呼吸脉冲指示灯（如 `.pulse-led-emerald`, `.pulse-led-amber`, `.pulse-led-rose`），禁止突兀的闪烁。
  - **定制纸质滑块与开关 (Paper Sliders & Toggles)**：严禁直接使用浏览器默认的粗糙灰色 Range Slider 与原生复选框，必须统一使用 2px 细线轨道与白底黑边圆点手柄（`.taste-slider`），滑动时具备微触觉阻尼感。
  - **轻量悬浮提示气泡 (Taste Tooltips)**：复杂控制信号与寄存器名称采用纯 CSS 悬浮信息气泡（`.taste-tooltip`），鼠标悬停时平滑浮现微操作语义定义，保持界面整洁同时降低学习门槛。
  - **零外部构建依赖**：所有微组件必须为纯 Tailwind CSS 类名或直接内嵌于模块的 `style.css`，严禁引入 npm 构建或外部 UI 框架。

---

## §4 解说与内容规范

- **408 演示**：无需解说。
- **数学演示**：只需解说本质即可，不堆砌大段公式。

---

## §5 工程与架构规范

- **零编译便携架构**：
  - 技术栈：HTML5 + Tailwind CSS (CDN) + Three.js / Canvas (CDN) + GSAP (CDN) + KaTeX (CDN) + 原生 ES6+ JavaScript；
  - 标准项目目录结构：
    ```text
    学科项目名称/
    ├── index.html   # 页面骨架与 HUD 布局
    ├── style.css    # Taste-Skill 浅色与 Callout 样式
    └── app.js       # 3D 场景、动态计算与 KaTeX 联动
    ```
  - 严禁引入 Webpack / Vite / npm 等重型打包依赖，确保双击 HTML 或运行轻量静态服务器（如 `python3 -m http.server`）即可开箱即用。
